@model List<IndexCategoryViewModel>

@section TitleSection {
    Categories
}

<div class="card mb-4">
    <div class="card-header">
        <h3 class="card-title">Show All Categories</h3>
    </div>
    <!-- /.card-header -->
    <div class="card-body p-0">
        <table class="table table-striped" role="table">
            <thead>
                <tr>
                    <th style="width: 10px" scope="col" class="text-center">#</th>
                    <th scope="col" class="text-center">Name</th>
                    <th scope="col" class="text-center">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="align-middle">
                        <td class="text-center">
                            <div class"d-flex justify-content-center gap-2">
                                @item.Id
                            </div>
                        </td>
                        <td class="text-center">
                            <div class"d-flex justify-content-center gap-2">
                                @item.Name
                            </div>
                        </td>
                        <td class="text-center">
                            <div class"d-flex justify-content-center gap-2">
                                <a asp-controller="Categories" asp-action="Delete" asp-route-id="@item.Id"
                                   class="btn btn-outline-danger">Delete</a>
                                <a asp-controller="Categories" asp-action="Edit" asp-route-id="@item.Id"
                                   class="btn btn-outline-info">Edit</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <!-- /.card-body -->
</div>